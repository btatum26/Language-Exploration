[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "registry-aligner"
version = "0.1.0"
description = "Backend-independent registry ingestion and forced-alignment CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.16,<2",
    "epitran>=1.35,<2",
    "psycopg[binary]>=3.2,<4",
    "pydantic>=2.11,<3",
    "python-dotenv>=1.1,<2",
    "rich>=14,<16",
    "sqlalchemy>=2.0,<3",
    "typer>=0.16,<1",
]

[project.scripts]
registry-align = "registry_align.cli:app"

[dependency-groups]
dev = [
    "mypy>=1.16,<1.18",
    "pytest>=8.4,<9",
    "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/registry_align"]

[tool.pytest.ini_options]
addopts = "-ra -p no:cacheprovider"
testpaths = ["tests"]
markers = ["postgresql: requires TEST_DATABASE_URL pointing to disposable PostgreSQL"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src"]
